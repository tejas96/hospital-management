import { Box } from "@material-ui/core";
import React from "react";
interface IProps {}
const RequestedItems: React.FC<IProps> = () => {
  return <Box>RequestedItems</Box>;
};

export default RequestedItems;
